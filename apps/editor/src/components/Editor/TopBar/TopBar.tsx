import { Box } from '@mui/material';
import { Toolbar } from './Toolbar';

export const TopBar = () => {
  return (
    <Box>
      {/* MenuBar will go here later */}
      <Toolbar />
    </Box>
  );
};
